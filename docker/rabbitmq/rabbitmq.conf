# lnk.day RabbitMQ 配置 (开发环境)

# 端口配置
listeners.tcp.default = 60036
management.tcp.port = 60037

# 日志级别
log.console.level = info

# 内存限制
vm_memory_high_watermark.relative = 0.6

# 磁盘限制
disk_free_limit.absolute = 1GB

# 心跳间隔 (60秒)
heartbeat = 60
